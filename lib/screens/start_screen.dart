import 'package:flutter/material.dart';

class CEEStartScreen extends StatelessWidget {
  const CEEStartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("cee health"),
      ),
    );
  }
}
